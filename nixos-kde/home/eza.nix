{
  programs.eza = {
    enable = true;  # Устанавливает eza в систему
    enableBashIntegration = true; # Добавляет алиасы в ~/.bashrc
    colors = "always"; # Всегда использовать цвета
    icons = "always"; # Всегда показывать иконки
  };
}
